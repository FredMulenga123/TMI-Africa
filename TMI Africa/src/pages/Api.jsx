import { Card } from '../components/Cards';import { apiEndpoints } from '../data/sampleData';
export default function Api(){return <div className="space-y-6"><Card><h1 className="text-2xl font-bold">TMI Africa API Layer</h1><p className="text-slate-500 mt-2">Endpoint templates are included under Netlify Functions. Replace mock logic with your database and authentication provider.</p></Card>{apiEndpoints.map(e=><Card key={e.path}><div className="font-mono text-sm"><span className="font-bold">{e.method}</span> {e.path}</div><p className="mt-2 text-slate-600">{e.purpose}</p></Card>)}</div>}
